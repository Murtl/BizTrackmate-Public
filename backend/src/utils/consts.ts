/**
 * Name der Collection in Firestore für Artikelgruppen.
 */
export const ARTICLE_GROUP_COLLECTION : string = 'articleGroups';
/**
 * Name der Collection in Firestore für Stores.
 */
export const STORES_COLLECTION: string = 'stores';

/**
 * Name der Collection in Firestore für Nutzer.
 */
export const USERS_COLLECTION : string = 'users';

/**
 * Name der Collection in Firestore für Lagerplätze.
 */
export  const STORAGE_SPACE_COLLECTION : string = 'storageSpaces';

/**
 * Name der Collection in Firestore für Artikel.
 */
export const ARTICLE_COLLECTION: string = 'articles';

/**
 * Name der Collection in Firestore für Transaktionen.
 */
export const TRANSACTION_COLLECTION: string = 'transactions';